import type {Instance} from '../src/types';
import {domains,users} from './catalog';
// 试点域：财务、人力资源、IT服务（新申请走新版本 v2）；采购、法务为现网 v1
const pilotDomains=new Set(['财务','人力资源','IT服务']);
const pilotStart=new Set(['财务','人力资源','IT服务']); // 试点于 2026-07-05 00:00 开始
export const instances:Instance[]=Array.from({length:80},(_,i)=>{const status:Instance['status']=i<12?'abnormal':i<22?'timeout':i<50?'running':'completed';const day=10-i%9;const domain=domains[i%5];const inPilot=pilotDomains.has(domain);const afterCutoff=pilotStart.has(domain)&&day>=5;const execVersion=inPilot&&afterCutoff?2:1;const pilot=inPilot?(execVersion===2?'pilot':'standard'):'standard';return {id:`INS-2026-${String(i+1).padStart(4,'0')}`,workflowId:`wf-${i%12+1}`,applicant:users[i%8],domain,currentNode:i%3===0?'直属主管审批':'金额判断',status,submittedAt:`2026-07-${String(day).padStart(2,'0')} ${String(8+i%10).padStart(2,'0')}:10`,duration:status==='timeout'?`${28+i}h`:`${i%9+1}h ${i%6*10}m`,risk:i<22?'high':i<45?'medium':'low',execVersion,pilot,timeline:[{title:'提交申请',time:'09:10',status:'completed'},{title:'直属主管审批',time:'10:24',status:i%3===0?'current':'completed'},{title:'金额判断',time:'11:05',status:i%3!==0?'current':'pending'}]};});
